"""CADE core — shared platform consumed by CADE and downstream forks (e.g. Padarax).

See docs/architecture/cade-core-subtree (or the Padarax custom-client plan §6)
for the contract: nothing in this tree may depend on IDE-specific code in
backend/ or frontend/src/ outside core/.
"""

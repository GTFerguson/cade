# Editor Integration

When the user has Neovim open in CADE, your `write_file` and `edit_file` calls automatically open or reload the affected file in their editor. You don't need to tell the user to refresh or open the file.

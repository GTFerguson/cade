# Bundled Rules

These rules are bundled with CADE and represent core guidance that applies to all projects.

**Do not modify files in this directory** — they will be overwritten when CADE is updated.

For user-specific rules, add `.md` files to `~/.claude/rules/` instead. User rules with different names than bundled rules will be loaded alongside these defaults.
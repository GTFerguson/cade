# Bundled Skills

These skills are bundled with CADE and represent core functionality available to all projects.

**Do not modify files in this directory** — they will be overwritten when CADE is updated.

For user-specific skills, create directories under `~/.claude/skills/` instead. User skills with different names than bundled skills will be loaded alongside these defaults.
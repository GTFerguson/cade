"""Agent memory capture layer.

Writes markdown memory files into `.cade/memory/` for nkrdn to ingest on
rebuild. Schema matches `nkrdn/parsers/memory/parser.py` exactly — that
parser is the source of truth for frontmatter shape.
"""
